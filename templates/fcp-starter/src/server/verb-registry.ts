/**
 * Verb registry — defines the operations your server supports.
 *
 * Each verb maps a command name to its handler, positional args,
 * and named parameters.
 *
 * See fcp-core/spec/grammar.md for the full grammar specification.
 */

// import { VerbDefinition } from '@ostk-ai/fcp-core';

// TODO: Define your verbs
// export const VERBS: VerbDefinition[] = [
//   {
//     name: 'add',
//     description: 'Add a new element',
//     positionals: ['type', 'label'],
//     params: {
//       // key: { type: 'string', description: '...' }
//     },
//   },
// ];

export const VERBS: unknown[] = [];
